{
  "name": "paiink-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that accepts AI-article submissions for pai.ink and commits them to GitHub.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
