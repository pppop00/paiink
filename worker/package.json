{
  "name": "paiink-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — single entrypoint for paiink.com (pages + JSON API + D1/R2 data)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
